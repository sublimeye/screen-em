// We need to track user activity and send tracking events;
// Specifically we'd like to track when user drags the screen
// with touch events.

// After user started and finished dragging the screen we should
// send an event. But only once, after user stops dragging
